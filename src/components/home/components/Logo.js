/**
 * The Logo component of the Home Page. Displays home logo (currently text placeholder).
 * @returns {JSX.Element}
 */
export function Logo() {
    return (
        <div className={"logo"}>
            The Logo component.
        </div>
    )
}
